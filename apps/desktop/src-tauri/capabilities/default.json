{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default capability for the RootCellar desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
